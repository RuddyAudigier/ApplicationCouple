<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="match_parent">

  <LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="Réglages Widget"
      android:textStyle="bold"
      android:textSize="20sp"
      android:paddingBottom="8dp" />

    <EditText
      android:id="@+id/appUrl"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:hint="URL app web (ex: https://application-couple.vercel.app)"
      android:inputType="textUri"
      android:padding="12dp" />

    <EditText
      android:id="@+id/apiUrl"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="10dp"
      android:hint="URL base API (ex: https://application-couple.vercel.app)"
      android:inputType="textUri"
      android:padding="12dp" />

    <EditText
      android:id="@+id/token"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="10dp"
      android:hint="POESIE_WIDGET_TOKEN"
      android:inputType="textPassword"
      android:padding="12dp" />

    <EditText
      android:id="@+id/recipient"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="10dp"
      android:hint="Email (destinataire)"
      android:inputType="textEmailAddress"
      android:padding="12dp" />

    <Button
      android:id="@+id/saveBtn"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:layout_marginTop="16dp"
      android:text="Enregistrer" />
  </LinearLayout>
</ScrollView>

